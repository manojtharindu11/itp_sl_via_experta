# Example environment configuration for ITP-SL
# Copy this file to .env and adjust values as needed.

# Optional: Use Mapbox instead of OpenStreetMap tiles in the UI
# Requires a Mapbox token. Defaults to OSM if not set.
USE_MAPBOX=false
MAPBOX_TOKEN=
# MAPBOX_STYLE=mapbox://styles/mapbox/streets-v11

# Optional: Custom OSM tile URL template (if you host your own tiles)
# OSM_TILE_URL=https://c.tile.openstreetmap.org/{z}/{x}/{y}.png
